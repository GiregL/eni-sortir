{% extends 'base.html.twig' %}

{% block body %}

{{ form_start(cancelForm) }}
        <h2>Annuler un événement</h2>
        
        <input type="hidden" name="token" value="{{ csrf_token('cancel-event-' ~ eventId) }}">
        {{ form_row(cancelForm.eventId) }}

        {{ form_label(cancelForm.cancelMessage) }}
        {{ form_widget(cancelForm.cancelMessage) }}
    {{ form_end(cancelForm) }}

{% endblock %}
{% extends 'base.html.twig' %}

{% block title %}New Event{% endblock %}

{% block body %}
{% include "partials/_flash.html.twig" %}
<main class="content-container" id="maincontent">
    <h1>Créer une sortie</h1>

    {{ form_start(form) }}
    <div class="stack">
        {{ form_widget(form) }}
    </div>
    {{ form_end(form) }}

    <a class="btn" href="{{ path('app_main') }}">back to home</a>
    </main>
{% endblock %}

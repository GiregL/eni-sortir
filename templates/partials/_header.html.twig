{% set menu = [
        {"link":url("app_main"), "label":"Accueil"}, 
    ]
%}
{%
    set disconnectedlinks = [
        {"link":"/login", "label":"Se connecter"},
        {"link":"/register", "label":"S'enregistrer"},
    ]
%}
{%
    set connectedLinks = [
        {"link":"/", "label":"Villes"}, 
        {"link":"/", "label":"Sites"}, 
        {"link":"/", "label":"Mon profil"},
        {"link":"/logout", "label":"Se déconnecter"},
    ]
%}
{% set menu = app.user? menu|merge(connectedLinks):menu|merge(disconnectedlinks) %}


<header class="container">
    <nav aria-labelledby="sections-heading">
        <h1><a href="">Sortir.com</a></h1>
        <ul>
        {% for item in menu %}
            <li><a href="{{item.link}}">{{item.label}}</a></li>
        {% endfor %}
        </ul>
    </nav>
</header>